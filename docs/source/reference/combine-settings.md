# Combine settings

Configuration for combining ORD results (`combine_settings_schema.json`), used when grouping
and aggregating result tables across analyses.

```{include} _generated/combine_settings.md
```
